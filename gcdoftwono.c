#include<stdio.h>
int main()
{
    int x,i,y,gcd;
    printf("enter two  integer");
    scanf("%d %d",&x,&y);
    
    for(i=1; i<= n1 && i<=n2; ++i)
    {
        if(n1%i==0 && n2%i==o)
          gcd=i;

      }
      printf(factorial of )
}
